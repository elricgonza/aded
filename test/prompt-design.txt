design

en la aplicación (archivo adjunto) mejorar el modulos de Calificaciones (templates/nota/index.html, templates/nota/form.html) y los que sean necesarios (se podría adicionar filtros de profesor y materia), de forma que el usuario con rol de "profesor" sólo pueda actualizar los cursos y materias asignadas, en cambio el usuario con rol de "transcriptor" ó "admin" puedan actualizar sin restricciones las calificaciones o notas

en el registro de pagos restringir de forma que los pagos se realicen de forma secuencial por el número de cuota (archivo templates/pago/form.html ó templates/pago/index.html)



----260527a
en la aplicación (archivo adjunto) modificar la actualización de notas y los archivos necesarios con las siguientes consideraciones:
-el usuario que ingresó (logueado) a la app, se vincula con la tabla profesor a través del campo 'usr_id_login' es decir que el id de usuario se encuentra en el campo "usr_id_login" de la tabla profesor, para determinar a que profesor corresponde el usuario y luego obtener el curso(s) y materia(s) asignadas al profesor.
-con la consideración descrita la opción EDITAR debe permitir si es que el profesor se encuentra asignado al curso y materia asignada.
Retornar sólo los archivos a corregir.

----260527b
En la aplicación (archivo adjunto) adicionar una opción en la aplicación (opción: Asigna Usuario como Profesor) que:
-Permita asociar la tabla USUARIO con la tabla PROFESOR (campo: usr_login_id), esta asociación implica que debe grabar el id de USUARIO en el campo: usr_login_id de la tabla PROFESOR
-que este disponible sólo para el usuario 'administrador' 
-Retornar sólo los archivos necesarios a adicionar y/o modificar.


----260527c
En la aplicación (archivo adjunto) adicionar las siguientes opciones en la aplicación que gestionan la tabla ASIGNADO, con las siguientes consideraciones:
-Primera parte y opción separada para  el poblado de la tabla ASIGNADO, generando registros de acuerdo a su estructura definida a partir de la tabla CURSO  (cur_id) con las materias (mat_id) que le corresponde según el grado al que pertenece el curso, identificando en la tabla materia por el campo: gra_id.
-Segunda parte y opción separada para complementar la tabla ASIGNADO con la asignación de profesor (pro_id) con el campo:id  de la tabla PROFESOR.
-que este disponible sólo para el usuario 'administrador' 
-Retornar sólo los archivos necesarios a adicionar y/o modificar.


----260528
-En la aplicación (archivo adjunto) mejorar la presentación de la opción 'Usuarios y Roles' admin/permisos.html, admin/roles.html, rol_form.html, usuario_form.html, usuarios.html, para que se vea de forma más profesional y alineada.
-que este disponible sólo para el usuario 'administrador'.
-Retornar sólo los archivos necesarios a adicionar y/o modificar.

----260613
-En la aplicación (archivo adjunto) adicionar la opción: 'Asignación de Costos' que permita la adición/edición de datos de costos con las siguientes características:
-Para cada curso (tabla: curso) debe crear 10 registros en la tabla "costo" donde: 
	--el campo: "id" llave de la tabla  debe ser generada automáticamente
	--el campo: "cur_id" corresponde al curso para el cual se define el costo por cuotas durante una gestión que tendrá 10 cuotas
	--el campo: "nro_cuota" es un número secuencial del 1 al 10 (debe ser generado automáticamente)
	--el campo: "cuota" es introducido por el usuario, campo numeral
	--el campo: "obs" es introducido por el usuario
	--el campo: "creado" fecha de creación llenado automáticamente por el sistema
	--el campo: "creado" fecha de actualización llenado automáticamente por el sistema
	--el campo: "usu_id" id del usuario que introduce los datos
-que este disponible sólo para el usuario 'administrador'.
-Retornar sólo los archivos necesarios a adicionar y/o modificar.

----260603
-En la aplicación (archivo adjunto) adicionar la opción: 'Genera Plan de Pagos' que permita la generación automática de registros en la tabla "pago", con las siguientes consideraciones a partir de las tablas: 'inscrito' y 'costo'.
-Para cada alumno inscrito (en la tabla: 'inscrito') y además que  el campo 'reserva' o el campo 'inscrito' esté en True  generar su plan de pagos en la tabla: 'pago' creando la misma cantidad de registros que tiene el curso del alumno en la tabla 'costo', donde, los campos de la tabla 'pago' a generar son:
	--el campo: "id" llave de la tabla  debe ser generada automáticamente
	--el campo: "ins_id" corresponde a la llave de la tabla 'inscrito' para cada alumno inscrito
	--el campo: "nro_cuota" es el mismo número secuencial del 1 al 10 que figura en la tabla 'costo' para el curso en el que fue inscrito el alumno campo 'cur_id' de la tabla 'curso'
	--el campo: "cuota" es el mismo monto que figura en la tabla 'costo' para el curso en el que fue inscrito el alumno, EXCEPTO en los casos en que el alumno inscrito (tabla 'inscrito' campo descuento mayor a 0) tiene descuento, se asignará el valor del campo cuota restando el el valor del porcentaje campo 'descuento', es decir será igual a la siguiente expresion:  
									cuota = cuota - (descuento*100)/cuota
	--el campo: "pagado" asignado en false inicialmente
	--el campo: "metodo_pago" asignar vacio inicialmente
	--el campo: "fecha_pago" asignar sin fecha inicialmente
	--el campo: "referencia_pago" asignar vacio inicialmente
	--el campo: "obs" asignar vacio inicialmente
	--el campo: "creado" fecha de creación llenado automáticamente por el sistema
	--el campo: "act" fecha de actualización llenado automáticamente por el sistema
	--el campo: "usu_id" id del usuario que introduce los datos
-que este disponible sólo para el usuario 'administrador'.
-Retornar sólo los archivos necesarios a adicionar y/o modificar.



Seguimos donde lo dejamos ayer. 🔥

La sala del directo ya está disponible.

Entrad, poneos cómodos y saludad en el chat, porque empezamos la segunda jornada del Curso de IA: de 0 a Agentes.

Hoy toca crear. Y vais a ver cosas muy potentes.

https://youtube.com/live/emYG-03K0lk?feature=share

Nos vemos en nada.